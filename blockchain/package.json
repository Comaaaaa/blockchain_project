{
  "name": "tokenimmo-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for TokenImmo - Tokenized Real Estate Platform",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network sepolia",
    "deploy:sepolia:lowcost": "ETH_EUR_RATE=5000000 NFT_LISTING_PRICE_ETH=0.001 INITIAL_LIQUIDITY_TOKENS=1 DEMO_LISTING_TOKENS=1 npx hardhat run scripts/deploy.js --network sepolia",
    "setup:uniswap:sepolia": "npx hardhat run scripts/setup-uniswap-v2.js --network sepolia",
    "node": "npx hardhat node"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.19.0"
  }
}
